--------------------------------------------------------------------------------
-- Cabal project file for prototypes
-- -----------------------------------------------------------------------------
--
-- The prototypes in this directory ideally only dependend on basic packages
-- such as `base` or `containers`.
--
-- Dependencies that are used are typically not related to cardano-wallet.
--
-- These prototypes are separate from the main project build.
--
--------------------------------------------------------------------------------
--
-- Example build command:
--
--   cabal build all --enable-tests --enable-benchmarks -O0
--
--------------------------------------------------------------------------------

index-state: 2022-01-01T00:00:00Z
with-compiler: ghc-8.10.7
packages: */*.cabal
