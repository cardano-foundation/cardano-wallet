# Functions model — #5397 L0

No new wallet function is designed by this ticket. Any signature adjustment is
an upstream API compatibility adaptation within an L0 package and must preserve
the pre-existing unsupported-Dijkstra behavior.
