# Modules model — #5397 L0

No new component is introduced. Existing L0 package modules retain ownership.
Any source edit is limited to a mechanical adaptation at the upstream API
boundary selected by the 11.1.0 pin set. The dependency-constraint repair, if
needed, belongs to the existing top-level `cabal.project` resolver boundary.
